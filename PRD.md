# VibeController MVP 产品需求文档

> 版本：v1.0.0<br>
> 日期：2026-07-18<br>
> 首发平台：Windows 10/11<br>
> 首发设备：Xbox Wireless Controller（Xbox Series X|S 手柄）、PS5 DualSense<br>
> 目标应用：Codex Windows 桌面端

## 1. 产品概述

VibeController 是一款轻量级 Windows 桌面软件，将游戏手柄输入转换为 Codex 可识别的键盘快捷键和鼠标事件，让用户在阅读代码、审阅改动、切换任务、确认/取消操作和语音输入时减少对键盘鼠标的依赖。

v1.0 支持 Xbox Series X|S 无线手柄和 PS5 DualSense。手柄可通过 Windows 已支持的蓝牙或 USB 连接，Xbox 还可使用 Xbox Wireless Adapter；VibeController 不负责实现蓝牙配对，只负责发现 Windows 已连接的手柄并绑定其输入。

语音能力不自建语音识别，也不集成 Typeless。手柄触发 Codex 桌面端“切换听写”操作；VibeController 首次使用时读取当前用户在 Codex 中的实际快捷键。声音由电脑当前默认麦克风采集，识别与文字写入由 Codex 完成。

## 2. 背景与机会

Codex 的大量高频操作本质上可以通过快捷键完成，但传统键盘并不适合以下场景：

- 用户正在阅读或思考，希望后仰、站立或离开标准打字姿势操作 Codex。
- 用户只需完成少量重复操作，例如听写、发送、删除文字、移动光标、切换任务和打开命令菜单。
- 用户希望用已有硬件获得类似专用控制器的体验，而不购买额外小键盘。
- 用户希望未来继续接入电视遥控器、小型蓝牙键盘等设备。

参考视频验证了“控制器按键触发 Codex 操作、电脑麦克风负责语音输入”的交互价值。VibeController 的差异点是：无需改装硬件、映射过程可视化、面向普通 Windows 用户，并为后续多设备适配保留统一输入层。

## 3. 产品目标

### 3.1 MVP 目标

1. 用户能够在 Windows 完成手柄连接后，启动软件即识别 Xbox 手柄。
2. 用户能够通过可视化界面查看并修改手柄按键映射。
3. 用户能够用手柄可靠触发 Codex 快捷键、键盘按键和基础鼠标操作。
4. 用户能够用手柄启动和结束 Codex 原生听写，并用另一按键发送内容。
5. 用户能够安全地启用、暂停和恢复映射，避免在游戏或其他应用中误触发。
6. 软件在断线、休眠和重连后能够自动恢复工作。

### 3.2 成功定义

一个首次使用的用户应能在 3 分钟内完成：

```text
打开软件
→ 看到已连接的 Xbox 手柄
→ 加载 Codex 默认配置
→ 按手柄键启动听写
→ 说出指令
→ 结束听写
→ 按手柄键发送给 Codex
```

## 4. 非目标

以下内容不进入 MVP：

- 自研语音识别、Whisper、Typeless 或其他第三方听写服务。
- 直接调用 Codex 私有 API、内部指令或未公开接口。
- Codex Agent 权限、推理强度等没有稳定快捷键的界面操作自动化。
- 读取 Codex 的内部任务状态、审批状态或生成结果。
- PS5 DualSense、TCL 遥控器及其他 HID 设备适配。
- macOS、Linux、Xbox 主机和移动端支持。
- 多设备同时控制、多人模式或云端配置同步。
- 任意脚本执行、复杂宏、条件分支和自动化工作流。
- 手柄固件升级、驱动安装或 Windows 蓝牙配对。

## 5. 目标用户与核心场景

### 5.1 目标用户

- 在 Windows Codex 桌面端长期工作的开发者或 AI 重度用户。
- 已拥有 Xbox Series X|S 手柄，希望复用现有硬件的用户。
- 经常使用语音描述需求、审阅代码或确认 Agent 操作的用户。

### 5.2 核心场景

#### 场景 A：语音下达任务

用户激活 Codex，按手柄上的听写键启动原生听写，对电脑麦克风说话，再次按键结束听写，最后按发送键提交。

#### 场景 B：轻量导航

用户使用肩键切换上一个/下一项 Codex 任务，使用右摇杆方向键在输入框或菜单中移动，使用 A 确认；需要取消时可在映射页自行绑定 `Esc`。

#### 场景 C：命令菜单操作

用户用手柄打开 Codex 命令菜单，再用右摇杆方向键和 A 选择已有命令。

#### 场景 D：鼠标补充操作

当某个 Codex 控件没有快捷键时，用户使用左摇杆移动鼠标、扳机键点击；输入框内的光标移动由右摇杆方向键完成。

#### 场景 E：安全暂停

用户准备玩游戏或使用其他软件时，从托盘或应用首页暂停映射。手柄仍连接 Windows，但 VibeController 不再注入任何键鼠事件。

## 6. 产品原则

1. **默认安全**：默认只在 Codex 位于前台时执行映射。
2. **反馈明确**：每次物理输入、映射命中和输出派发都应有可见反馈。
3. **配置简单**：默认配置开箱即用，高级配置逐步展开。
4. **不依赖云端**：除 Codex 自身能力外，VibeController 不上传输入或语音数据。
5. **设备与动作解耦**：Xbox 输入先转换为统一事件，再映射为动作，为 TCL 遥控器适配留出接口。
6. **克制而有质感**：前端遵循 `emilkowalski/skills` 中的 Apple Design 原则，以即时反馈、空间一致性、清晰层级和可打断动效建立“苹果味儿”，不做表面化的拟物复制。

## 7. MVP 功能范围

### 7.1 P0：设备发现与状态

#### FR-01 发现手柄

- 读取 Windows 已连接的 Xbox 兼容控制器。
- 显示设备名称、连接状态和最近活动时间。
- MVP 同时只允许一个活动控制器。
- 检测到多个控制器时允许用户选择一个。

#### FR-02 连接状态

状态包括：

- 未发现设备
- 已连接、映射已启用
- 已连接、映射已暂停
- 设备已断开
- 输入读取异常

#### FR-03 自动重连

- 手柄休眠、断开或超出蓝牙范围后，软件保持运行。
- 同一设备重新连接后自动恢复选择。
- 若映射在断线前为暂停状态，重连后仍保持暂停。

### 7.2 P0：实时输入测试

#### FR-04 手柄可视化

- 界面展示 Xbox 手柄示意图。
- 用户按下实体按键时，对应控件实时高亮。
- 摇杆显示方向和幅度，扳机显示按压比例。

#### FR-05 事件检查器

- 显示最近一次输入名称、按下/释放状态和时间。
- 显示该输入当前绑定的动作。
- 提供“测试模式”；测试模式只显示输入，不发送键鼠事件。

### 7.3 P0：映射编辑器

#### FR-06 支持的手柄输入

MVP 至少支持：

- A、B、X、Y
- 方向键上、下、左、右
- LB、RB
- LT、RT（按阈值转为数字事件）
- 左摇杆、右摇杆
- L3、R3
- Menu、View
- DualSense 的 □、×、○、△、L1/R1、L2/R2、Options/Create 和触控板

Xbox Guide 键可能被 Windows 或 Xbox Game Bar 保留，不作为 MVP 可保证输入。Share 键如底层接口能够稳定读取则展示，否则不阻塞发布。

#### FR-07 支持的输出动作

MVP 支持以下动作类型：

1. 内置 Codex 动作
   - 激活 Codex 窗口
   - 切换听写快捷键
   - 发送消息
   - Esc（取消/返回，可选）
   - 打开命令菜单
   - 上一个任务
   - 下一项任务
   - 上一个/下一个最近查看的任务
   - 上一个/下一个标签页
   - 降低推理强度
   - 提高推理强度
2. 键盘动作
   - 单个按键
   - 带 Ctrl、Shift、Alt、Win 修饰键的组合键
3. 鼠标动作
   - 移动鼠标
   - 左键点击
   - 右键点击
   - 垂直滚动
4. 空动作
   - 明确设为“不执行任何操作”

#### FR-08 快捷键录入

- 用户选择“键盘快捷键”后，可直接按键盘录入组合键。
- 界面以可读形式展示，例如 `Ctrl + Alt + D`。
- 不允许只绑定孤立的 Ctrl、Shift、Alt 或 Win。
- 保存前检查重复映射并提示，但允许用户确认后继续。

#### FR-09 摇杆参数

- 左摇杆可映射为鼠标移动。
- 右摇杆四方向默认映射为键盘方向键，越过阈值立即触发，按住自动重复，回中停止。
- 用户可设置鼠标速度、滚动速度和死区。
- 默认提供平滑和加速曲线，避免光标抖动。

#### FR-10 配置持久化

- 修改后自动保存到本机。
- 重启软件后恢复上次配置。
- 提供“恢复 Codex 默认配置”。
- MVP 只维护一个当前配置，不提供多配置档案管理。

### 7.4 P0：Codex 操作

#### FR-11 激活 Codex

- 提供“激活 Codex”内置动作，将已运行的 Codex 桌面窗口带到前台。
- 未找到 Codex 时不向其他窗口发送后续输入，并显示明确提示。
- MVP 不负责自动启动或登录 Codex。

#### FR-12 Codex 原生听写

- 首次触发时读取 Codex 快捷键设置中的“切换听写”实际绑定；Codex 配置变化后自动刷新。
- 如果该动作没有绑定，显示明确提示，不发送硬编码的回退按键。
- VibeController 不访问麦克风，不采集、不缓存、不上传音频。
- 录音设备、麦克风权限、语音识别和转写结果全部由 Codex 负责。
- MVP 将听写设计为“按一次触发一次快捷键”；开始/结束行为遵循当前 Codex 桌面端实现。
- 界面不声称已经开始录音，只提示“已发送听写指令”，避免把快捷键派发成功误判为 Codex 录音成功。

#### FR-13 发送、删除与取消

- “发送”读取 Codex 的 `composer.submit` 绑定；未自定义时使用 Codex 默认的 `Enter`。
- “删除上一个字符”默认映射为 `Backspace`。
- “取消/返回”以 `Esc` 保留在可选映射动作中，但默认不占用手柄按键。
- 所有输出只在 Codex 前台保护通过时发送。

#### FR-14 Codex 导航

- “打开命令菜单”、任务、最近任务、标签页和推理强度动作均读取 Codex 当前用户的快捷键设置。
- 用户自定义绑定优先于 Codex 默认绑定；明确取消绑定时不得使用默认值。
- Codex 没有默认快捷键的动作（例如推理强度）必须先在 Codex 设置中绑定。
- 不支持的快捷键格式显示错误，并引导用户改为常规 Windows 组合键。

### 7.5 P0：执行范围与安全

#### FR-15 执行范围

提供两个模式：

- **仅 Codex 前台执行（默认）**：只有 Codex 是当前前台窗口时才发送动作，“激活 Codex”除外。
- **全局执行**：无论当前应用是什么都执行映射，界面需提示误输入风险。

#### FR-16 总开关

- 首页提供明显的“启用映射/暂停映射”开关。
- Windows 系统托盘提供相同开关。
- 暂停后仍读取手柄状态用于界面测试，但不注入键鼠事件。
- 托盘图标需区分启用、暂停和未连接状态。

#### FR-17 防重复触发

- 普通按键一次按下只触发一次动作，持续按住不重复触发，除非该动作明确需要连续执行。
- 方向键和滚动允许可控连发，并设置初始延迟和连发间隔。
- LT/RT 使用进入阈值和释放阈值，避免临界值抖动造成连击。

#### FR-18 权限边界

- 软件默认以普通用户权限运行，不主动申请管理员权限。
- 当 Codex 或目标窗口以更高权限运行而导致输入注入失败时，显示原因和解决提示。
- 不绕过 Windows 安全边界。

### 7.6 P1：反馈与诊断

#### FR-19 动作反馈

- 首页短暂显示最近执行的动作，例如“X → 切换听写快捷键”。
- 输入被前台保护拦截时，显示“Codex 未处于前台，动作未发送”。
- 可选使用短震动表示动作已派发；震动仅表示 VibeController 已执行，不代表 Codex 已成功处理。

#### FR-20 诊断信息

- 提供最近 50 条本地事件记录。
- 记录设备连接、断开、按键命中、动作派发和错误。
- 默认不记录用户在 Codex 中输入的文本内容。
- 支持复制诊断信息，方便反馈问题。

### 7.7 P1：应用生命周期

#### FR-21 托盘运行

- 关闭主窗口时默认最小化到系统托盘。
- 托盘菜单包含：打开 VibeController、启用/暂停映射、退出。

#### FR-22 开机启动

- 设置中提供“登录 Windows 后自动启动”。
- 默认关闭，由用户主动开启。

## 8. 默认 Xbox 映射

首次启动时加载以下默认配置：

| Xbox 输入 | 默认动作 | 实际输出/行为 |
| --- | --- | --- |
| Menu | 激活 Codex | 将 Codex 窗口带到前台 |
| X | 切换听写快捷键 | 读取 Codex 当前绑定 |
| A | 发送/确认 | 读取 Codex 当前绑定，默认 `Enter` |
| B | 删除上一个字符 | `Backspace` |
| Y | 打开命令菜单 | 读取 Codex 当前绑定 |
| LB | 上一个任务 | 读取 Codex 当前绑定 |
| RB | 下一项任务 | 读取 Codex 当前绑定 |
| 方向键左/右 | 降低/提高推理强度 | 读取 Codex 当前绑定 |
| 方向键上/下 | 纵向导航 | 对应键盘方向键 |
| 左摇杆 | 移动鼠标 | 鼠标相对移动 |
| RT | 鼠标左键 | 左键单击 |
| LT | 鼠标右键 | 右键单击 |
| 右摇杆四方向 | 移动输入光标 | 对应键盘方向键，按住自动重复 |
| View、L3、R3 | 未分配 | 无动作 |

默认映射属于起点，不代表所有用户的最佳方案。用户可以修改除系统保留键之外的所有映射。

## 9. 关键用户流程

### 9.1 首次使用

```text
启动 VibeController
→ 显示“正在寻找 Xbox 手柄”
→ 检测到设备
→ 展示手柄示意图和 Codex 默认配置
→ 引导用户按 A 确认输入正常
→ 引导用户打开 Codex
→ 按 X 测试听写
→ 完成设置
```

若未发现设备：

```text
提示先在 Windows 设置中完成配对
→ 提供“重新检测”按钮
→ 提供 USB/蓝牙/适配器基础排查说明
```

### 9.2 修改映射

```text
进入“按键映射”
→ 点击手柄图上的按键，或直接按实体按键
→ 选择内置动作/键盘/鼠标/无动作
→ 如为自定义快捷键，按键盘录入
→ 保存并立即生效
→ 在测试区验证
```

### 9.3 使用听写

```text
按 Menu 激活 Codex
→ 按 X 触发 Codex 原生听写
→ 对电脑麦克风说话
→ 按 X 再次触发听写快捷键
→ 检查生成文本
→ 用右摇杆移动光标、按 B 删除，确认后按 A 发送
```

## 10. 信息架构与界面

### 10.1 首页

首页需要一眼回答三个问题：设备是否在线、映射是否启用、刚才的按键是否生效。

主要区域：

- 当前设备卡片
- 启用/暂停总开关
- Xbox 手柄实时示意图
- 最近动作反馈
- “测试输入”入口
- “编辑映射”主按钮

### 10.2 按键映射页

主要区域：

- 左侧：可交互 Xbox 手柄图
- 右侧：当前选中按键的动作配置
- 下方：实时输入与输出预览
- 顶部：恢复默认配置

### 10.3 设置与诊断页

包括：

- 仅 Codex 前台执行/全局执行
- Xbox / PS5 DualSense 设备切换
- 鼠标速度、滚动速度、摇杆死区
- Codex 快捷键自动同步状态与说明
- 托盘与开机启动设置
- 设备与版本信息
- 本地事件记录

### 10.4 视觉与交互设计规范

前端实现必须使用 [emilkowalski/skills](https://github.com/emilkowalski/skills) 作为设计与动效评审基线，重点采用其中：

- `apple-design`：Apple 风格的界面原则、材质、空间关系、即时反馈和流畅动效。
- `emil-design-eng`：通用界面设计和动效实现规范。
- `review-animations`：动效完成后的严格复查。

进入前端实现时再安装并记录所使用的仓库版本；PRD 阶段不把第三方 skill 打包进产品运行时。该 skill 只指导设计与开发，不成为最终软件的运行依赖。

具体设计要求：

- **气质**：安静、精确、轻盈、可信赖，避免“游戏外设控制面板”常见的高饱和 RGB 和复杂仪表盘风格。
- **平台适配**：保留 Apple 式克制和流动感，但仍遵循 Windows 桌面习惯；使用系统字体栈，Windows 优先使用 Segoe UI Variable，不依赖未授权的 SF Pro 字体。
- **层级**：首页只突出设备状态、映射总开关和手柄；高级参数收纳到二级页面，不把所有配置堆在首屏。
- **材质**：允许在导航栏、浮层和状态卡片中适度使用半透明、模糊和柔和阴影；不得多层叠加玻璃效果影响文字对比度。
- **颜色**：以中性色为主，连接成功、听写指令、暂停和错误使用少量语义色；颜色不能成为状态的唯一表达方式。
- **圆角与间距**：统一设计 token，不允许页面中出现无规则的圆角、阴影和间距值。
- **按压反馈**：按钮和手柄图上的按键在输入发生时立即反馈，不等待动作执行完成后才高亮。
- **动效**：使用自然、可打断的弹簧或短促缓动；默认临界阻尼、少回弹，只在具有动量的拖动场景中允许轻微回弹。
- **空间一致性**：面板从触发它的控件附近展开，并沿相同路径关闭；切换页面和弹层不得无缘由改变方向。
- **性能**：动效优先使用 `transform` 和 `opacity`，不得为了装饰阻塞输入或延迟按键反馈。
- **反馈协调**：视觉反馈和可选手柄震动应来自同一个动作事件；两者都只表示 VibeController 已派发操作。
- **可访问性**：尊重减少动态效果、高对比度和减少透明度设置；键盘可完整操作；焦点状态清晰可见。
- **文案**：使用直接、具体的动作名称，例如“暂停映射”“激活 Codex”，避免“开始体验”“智能控制”等空泛表达。

以下表现不符合设计要求：

- 为了“苹果味儿”照搬 macOS 窗口红黄绿按钮或移动端导航结构。
- 大面积、低对比度的玻璃拟态导致可读性下降。
- 每个按钮都有弹跳、发光或声音。
- 固定时长且不可中断的长动画。
- 为追求极简而隐藏连接状态、暂停状态或错误原因。

## 11. 状态与错误处理

| 情况 | 系统行为 | 用户提示 |
| --- | --- | --- |
| 未连接手柄 | 不执行映射，持续低频检测 | 请先在 Windows 中连接 Xbox 手柄 |
| 手柄休眠 | 保持配置，等待重连 | 手柄已断开，唤醒后将自动重连 |
| Codex 未运行 | 拦截 Codex 动作 | 未找到 Codex，请先启动 Codex |
| Codex 不在前台 | 默认拦截普通动作 | Codex 未处于前台；按 Menu 激活 |
| 快捷键注入失败 | 不自动重试，记录错误 | 操作未发送，请检查应用权限 |
| 配置损坏 | 备份损坏文件并恢复默认值 | 配置已恢复为默认设置 |
| 多个手柄在线 | 使用用户选择的活动设备 | 请选择要使用的手柄 |

## 12. 数据、隐私与安全

- 所有映射和设置仅保存在本机。
- VibeController MVP 不需要账户系统、云服务或网络连接。
- 软件不申请麦克风权限，不接触语音数据。
- 软件不记录用户听写内容、Codex 对话内容或剪贴板内容。
- 诊断日志只记录输入名称、映射动作、连接状态和错误，不记录实际文本。
- 不提供任意脚本或命令执行能力。

## 13. 非功能需求

### 13.1 性能

- 手柄按键到界面高亮延迟：P95 小于 50 ms。
- 手柄按键到键鼠事件派发延迟：P95 小于 100 ms。
- 空闲时 CPU 占用目标：小于 1%。
- 空闲时内存占用目标：小于 150 MB。

### 13.2 稳定性

- 连续运行 8 小时不崩溃、不重复注入输入。
- 手柄断开重连 20 次后仍可恢复映射。
- 应用异常退出不得导致按键或修饰键保持“按下”状态。

### 13.3 兼容性

- Windows 10 22H2 及更新版本。
- Windows 11 当前受支持版本。
- Xbox Series X|S 无线手柄通过蓝牙和 USB 作为必须验证路径。
- Xbox Wireless Adapter 作为应验证路径；若首版存在底层限制，应在发布说明明确标注。
- 软件以普通用户权限安装和运行。

### 13.4 可用性

- 首次启动无需阅读文档即可完成基础设置。
- 所有高风险全局输入模式必须有明确状态提示。
- 首版至少交付一套完整、清晰的浅色主题；深色主题不是 MVP 发布阻塞项。
- 所有交互动效通过 `review-animations` 规则复查，不存在装饰性过度动画、输入阻塞或不可打断的关键过渡。
- 开启 Windows 减少动态效果或高对比度设置时，核心功能和状态辨识不受影响。

## 14. MVP 验收标准

满足以下条件才可以认为 MVP 完成：

1. 蓝牙或 USB 连接的 Xbox Series X|S 手柄能被应用识别。
2. 所有 P0 手柄按键在测试模式中正确显示按下和释放。
3. 用户可修改、保存和恢复默认映射，重启后配置不丢失。
4. X 能读取当前用户在 Codex 中为“切换听写”设置的快捷键，并稳定触发该动作。
5. A、Y 和 LB/RB 能按 Codex 当前绑定执行对应语义动作，B 稳定发送 `Backspace`。
6. LB/RB 分别触发“上一个任务”和“下一项任务”，不会误触标签页切换。
7. 左摇杆、RT、LT 能完成基础鼠标移动与点击；右摇杆四方向能连续移动输入光标。
8. “仅 Codex 前台执行”模式下，不会向记事本、浏览器等其他应用误发输入。
9. 暂停映射后，任何手柄操作都不会产生键鼠输出。
10. 手柄休眠并重新唤醒后，无需重启应用即可继续使用。
11. 连续快速按键不会出现单次输入被重复发送的问题。
12. 软件不请求麦克风权限，且诊断日志中不存在听写文本。
13. 首页在不打开二级页面的情况下，能明确看到设备、映射和最近动作三类状态。
14. 实体手柄按下后，界面对应按键立即产生视觉反馈，且不因动效阻塞下一次输入。
15. 前端按照 `apple-design` 和 `review-animations` 完成一次设计审查，并关闭所有 P0/P1 视觉与动效问题。

## 15. 核心指标

MVP 内部测试关注：

- 首次连接成功率
- 首次设置完成时间
- 按键动作派发成功率
- 重复或漏触发率
- 断线自动恢复成功率
- Codex 原生听写 10 次连续触发成功率
- 从启动软件到首次成功发送语音指令的时间

发布门槛建议：

- 核心按键派发成功率不低于 99%。
- 10 次连续听写触发全部成功。
- 20 次断线重连全部自动恢复。
- 无 P0 级误输入、卡键或崩溃问题。

## 16. 技术边界建议

本节用于约束实现方向，不指定最终框架：

```text
Xbox Device Adapter
        ↓
Normalized Input Event
        ↓
Mapping Engine
        ↓
Safety Guard
        ↓
Keyboard / Mouse / Window Action Executor
```

- 设备适配层不得直接发送快捷键。
- 映射层只处理统一输入事件，不依赖 Xbox 专有按钮对象。
- 输出层统一释放按键状态，避免应用退出时产生“卡键”。
- TCL 遥控器未来只需新增 Device Adapter，不应重写映射编辑器和输出执行器。
- Codex 动作内部仍基于公开快捷键或普通 Windows 窗口能力，不依赖未公开接口。

## 17. 主要风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| Codex 快捷键未来变化 | 默认映射失效 | 首次使用时读取用户实际绑定，配置变化后自动刷新；未绑定时明确报错 |
| Codex 听写行为与预期不同 | 无法实现真正按住说话 | MVP 使用单次快捷键触发；以实际 Codex 行为验收 |
| Xbox Guide/Share 键被系统保留 | 部分按键不可用 | 不将其作为核心默认映射 |
| 蓝牙休眠或丢包 | 漏输入、断连 | 自动重连、状态提示、事件去抖 |
| 摇杆漂移 | 光标自行移动 | 默认死区、可调阈值和校准提示 |
| 输入注入到错误窗口 | 误操作其他软件 | 默认仅 Codex 前台执行，并提供总开关 |
| 管理员权限不一致 | 输入派发失败 | 普通权限运行并明确提示权限边界 |
| 用户同时运行游戏 | 控制器操作冲突 | 托盘快速暂停，状态始终可见 |

## 18. 后续版本方向

### v1.1

- 多配置档案及按应用自动切换。
- 长按、双击和组合键触发方式。
- 更丰富的震动反馈。
- 配置导入和导出。
- Codex 可用快捷键自动检查与引导。

### v1.2

- TCL 电视遥控器及通用蓝牙 HID 设备适配。
- 可视化设备模板和按键学习模式。
- 多设备共享同一动作配置。

### 未来探索

- 在公开且稳定的接口出现后接入更丰富的 Codex 原生动作。
- 可选内置语音识别提供方，但不改变 Codex 原生听写的零配置默认体验。
- 面向社区的设备适配器插件机制。

## 19. 待产品评审决策

以下问题不阻塞 PRD 评审，但应在进入实现前确认：

1. 默认听写交互是否接受“X 按一次开始、再按一次结束”，还是必须追求按住说话。
2. 鼠标模拟是否作为首版发布必需项，还是可以放到首个增量版本。
3. “全局执行”是否在 MVP 暴露给普通用户，还是仅保留 Codex 前台模式。
4. 首版是否需要安装包和自动更新，还是先以可执行程序形式内部试用。
5. 默认关闭窗口行为是最小化到托盘，还是直接退出。

## 20. 参考资料

- 本地参考视频：`参考视频.mp4`
- Codex 桌面端快捷键：<https://learn.chatgpt.com/docs/reference/commands.md>
- Xbox Wireless Controller：<https://www.xbox.com/en-US/accessories/controllers/xbox-wireless-controller>
- VibeRemote：<https://github.com/dmq1219/VibeRemote>
- 前端设计与动效 skill：<https://github.com/emilkowalski/skills>
- Apple Design skill：<https://github.com/emilkowalski/skills/blob/main/skills/apple-design/SKILL.md>
